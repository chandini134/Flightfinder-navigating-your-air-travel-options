import React from 'react';

const FlightRequests = () => {
  return (
    <div className="flight-requests-page">
      <h2>Flight Requests</h2>
      <p>List of pending flight approval requests will appear here.</p>
      {/* You can map flight requests here once the data is ready */}
    </div>
  );
};

export default FlightRequests;
